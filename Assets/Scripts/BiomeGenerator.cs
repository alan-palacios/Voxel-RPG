using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class BiomeGenerator{

/*
    public static float[,] GenerateBiomeMap(){
        float[,] wildNoise = Noise.GenerateNoiseMap (10, 10, );

        float[,] noiseMap = Noise.GenerateNoiseMap (mapChunkSize+1, mapChunkSize+1, biomeData.noiseData.seed,
                biomeData.noiseData.noiseScale, biomeData.noiseData.octaves, biomeData.noiseData.persistance,
                biomeData.noiseData.lacunarity, centre + biomeData.noiseData.offset, biomeData.noiseData.normalizeMode);

                return null;
    }*/
}
